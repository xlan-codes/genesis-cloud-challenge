
const dev = {
    baseUrl: 'http://localhost:4000',
    alarm: '/alarm',
    instance: '/instance',
    image: '/image',
    sshkeys: '/ssh-key',
    instanceType: '/instance-types'

}

const prod = {
    baseUrl: 'http://localhost:4000',
    alarm: '/alarm',
    instance: '/instance',
    image: '/image',
    sshkeys: '/ssh-key',
    instanceType: '/instance-types'
}

const constants = dev;
export default constants;
